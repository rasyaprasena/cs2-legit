# Contributing

1. Keep two projects under `src/` (App + Core). Prefer new `.cs` modules in Core over new projects.
2. No live RPC or fund movement in default builds.
3. Run `dotnet test cs2-legit.slnx -c Release` before PR.
